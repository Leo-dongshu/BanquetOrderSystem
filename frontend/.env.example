# 环境变量配置示例
# 复制此文件为 .env.development 或 .env.production 并根据需要修改

# API 基础地址
# 开发环境: 使用相对路径 /api (配合 vite 代理使用)
# 生产环境: 如果前端和后端部署在同一域名下，用 /api；如果不同域名，用完整地址 http://your-domain.com/api
VITE_API_BASE_URL=/api

# 开发环境代理目标 (仅 .env.development 使用)
VITE_PROXY_TARGET=http://localhost:8082
